# Dependency licensing

SoundPilot uses Angular, RxJS, Angular Material/CDK, Vitest, Storybook, ESLint and Prettier.
Their primary published licenses are MIT. Review the resolved dependency tree with `npm ls --all` and the respective package metadata before every release.
